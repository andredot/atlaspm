% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe.R
\name{flow_counts}
\alias{flow_counts}
\title{Analysis flow counts}
\usage{
flow_counts(
  mort_raw,
  mort_count,
  mort_count_area,
  area_shp,
  age_threshold = 75
)
}
\arguments{
\item{mort_raw}{Output of \code{\link[=import_mortality]{import_mortality()}}.}

\item{mort_count}{Output of \code{\link[=preprocess_mortality]{preprocess_mortality()}}.}

\item{mort_count_area}{\code{mort_count} restricted to modelled areas.}

\item{area_shp}{The modelling geography.}

\item{age_threshold}{Upper age bound used, for the label. Default \code{75}.}
}
\value{
A tibble: \code{step}, \code{n}, \code{lost}, with one row per stage.
}
\description{
The numbers behind a STROBE/RECORD flow diagram, and behind the bias
discussion: how many records the register supplied, how many survived each
restriction, and how many were lost at each step.
}
\details{
The external-causes exclusion is worth quantifying rather than asserting.
Discussion says deaths from chapters V-Y are absent from ReNCaM and that this
understates avoidable mortality; the size of the gap is the difference
between records that failed to match the lookup and records that matched.
}
\examples{
\dontrun{
flow_counts(mort_raw, mort_count, mort_count_area, area_shp)
}
}
