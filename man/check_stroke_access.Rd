% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{check_stroke_access}
\alias{check_stroke_access}
\title{Diagnose the stroke accessibility covariate before modelling}
\usage{
check_stroke_access(stroke_times, stroke_area, print = TRUE)
}
\arguments{
\item{stroke_times}{Tibble from \code{\link[=build_stroke_times]{build_stroke_times()}}.}

\item{stroke_area}{Tibble from \code{\link[=build_stroke_area]{build_stroke_area()}}.}

\item{print}{Whether to message the summary.}
}
\value{
A list with \code{centroid_bias}, \code{layer_variance}, and \code{headline}.
}
\description{
Two questions this answers, both of which should be settled before the
covariate enters a model.
}
\details{
\emph{How much would the centroid shortcut have cost?} The convexity gap between
the population-weighted mean of section times and the time at the area's
median section. Worth reporting in the methods as justification for routing
at section level rather than merely asserting that it matters.

\emph{Is there variance to model?} Milan holds four hubs and three spokes, so
every NIL sits minutes from a stroke centre. A covariate with a
sub-3-minute standard deviation across the NIL layer carries essentially no
signal, and a null coefficient there is a property of the geography rather
than evidence about access.
}
