% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deprivation.R
\name{build_section_area_xwalk}
\alias{build_section_area_xwalk}
\title{Crosswalk census sections to modelling areas}
\usage{
build_section_area_xwalk(sez_shp, area_shp, sez_key = NULL)
}
\arguments{
\item{sez_shp}{\code{sf} of ISTAT census section polygons.}

\item{area_shp}{\code{sf} of modelling areas carrying an \code{area} column.}

\item{sez_key}{Name of the section identifier column in \code{sez_shp}. Detected
automatically when \code{NULL}.}
}
\value{
A tibble with \code{sez_code} and \code{area}, one row per section that falls
inside a modelling area.
}
\description{
Assigns every census section polygon to the modelling area that contains it,
by spatial join on the section's point-on-surface. The same device
\code{\link[=build_section_points]{build_section_points()}} uses for the stroke origins, and for the same
reason: reconstructing the composite \code{area} key from ISTAT codes would break
the moment the key definition changes, whereas a spatial join cannot drift
out of step with \code{area_shp}.
}
\details{
Point-on-surface rather than centroid: a centroid can fall outside a concave
section, which for a section wrapping a city block would place it in the
neighbouring NIL.
}
\seealso{
\code{\link[=build_deprivation]{build_deprivation()}}, \code{\link[=build_section_points]{build_section_points()}}
}
