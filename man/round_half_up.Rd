% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{round_half_up}
\alias{round_half_up}
\title{Round half away from zero}
\usage{
round_half_up(x, digits = 0)
}
\arguments{
\item{x}{Numeric vector.}

\item{digits}{Number of decimal places. Default \code{0}.}
}
\value{
Numeric vector, rounded half away from zero.
}
\description{
Base R's \code{\link[=round]{round()}} implements IEEE 754 round-half-to-even ("banker's
rounding"), so \code{round(0.5)} is \code{0} and \code{round(2.5)} is \code{2}. The OECD/Eurostat
fractional allocation described in the methods rounds half \strong{up}, and the
difference is not academic: half-weight counts are exactly the values at
which the two rules disagree, and they are common in this data (every one of
the nine split causes produces them).
}
\examples{
round_half_up(c(0.5, 1.5, 2.5, -0.5))   # 1 2 3 -1
round(c(0.5, 1.5, 2.5, -0.5))           # 0 2 2  0
}
