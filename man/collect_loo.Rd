% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{collect_loo}
\alias{collect_loo}
\title{Leave-one-out comparison table, with the Pareto-k audit}
\usage{
collect_loo(comparison, k_threshold = 0.7)
}
\arguments{
\item{comparison}{A \code{bym2_comparison} from \code{\link[=compare_bym2]{compare_bym2()}}.}

\item{k_threshold}{Pareto-k value above which an observation is flagged.
Default \code{0.7}.}
}
\value{
A tibble: \code{model}, \code{elpd_loo}, \code{se_elpd}, \code{p_loo}, \code{looic},
\code{elpd_diff}, \code{se_diff}, \code{ratio}, \code{verdict}, \code{n_pareto_high}.
}
\description{
Wraps a \code{bym2_comparison} into the table Results asks for: models ordered by
expected log predictive density, elpd differences from the best with their
standard errors, and a verdict on whether each difference is larger than its
own uncertainty.
}
\details{
Also surfaces the Pareto-k counts, which Results quotes and which the print
method buries. A high k means the leave-one-out approximation is unreliable
for that area, and in this study the offenders are predictably the least
populous units - worth reporting rather than hiding.
}
