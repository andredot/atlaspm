% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_report.R
\name{plot_forest}
\alias{plot_forest}
\title{Forest plot of relative risks}
\usage{
plot_forest(
  coefs,
  facet = NULL,
  y = "label",
  xlab = "Relative risk per standard deviation (95\% credible interval)",
  subtitle = NULL,
  caption = NULL,
  null_line = 1
)
}
\arguments{
\item{coefs}{A tibble from \code{\link[=collect_coefficients]{collect_coefficients()}} or \code{\link[=collect_controls]{collect_controls()}}.}

\item{facet}{Optional faceting column, e.g. \code{"model"} or \code{"role"}.}

\item{y}{Column to use for the row labels. Default \code{"label"}.}

\item{xlab}{Axis label.}

\item{subtitle, caption}{Passed through to \code{labs()}.}

\item{null_line}{Where to draw the reference line. Default \code{1}.}
}
\value{
A ggplot.
}
\description{
The figure Results "Covariate association" is missing, and the one the
control panel needs. Log x-axis, because a relative risk of 0.5 and one of
2.0 are the same distance from the null and a linear axis would hide that.
}
\examples{
\dontrun{
plot_forest(collect_coefficients(fits), facet = "model")
}
}
