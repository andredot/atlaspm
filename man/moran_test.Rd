% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{moran_test}
\alias{moran_test}
\title{Moran's I on model residuals}
\usage{
moran_test(fit, geo, C, nsim = 9999, n_draws = 500, style = "W")
}
\arguments{
\item{fit}{A fitted model.}

\item{geo}{The \code{sf} it was fitted to.}

\item{C}{Adjacency matrix.}

\item{nsim}{Permutations for the p-value on the mean residual.}

\item{n_draws}{Draws to use for the posterior distribution of I. Default
\code{500}; the full posterior is rarely worth the time here.}

\item{style}{Weights style.}
}
\value{
A one-row tibble: \code{statistic} (on the posterior-mean residual),
\code{p_value}, and \code{post_mean} / \code{post_low} / \code{post_high} summarising I across
draws.
}
\description{
The post-model version. Confirms that the spatial term has absorbed the
structure that \code{\link[=moran_test_raw]{moran_test_raw()}} found in the crude ratios.
}
\details{
Two things this does that the ad-hoc version in the old \code{reports/explore.R}
did not. It works on \strong{Pearson residuals}, \eqn{(y - \mu)/\sqrt{\mu}}, not
on the smoothed relative risks: smoothed RRs are spatially autocorrelated by
construction, so testing them tells you only that the smoother smoothed. And
it propagates posterior uncertainty, returning the distribution of I across
draws alongside the permutation test on the posterior-mean residual.
}
\seealso{
\code{\link[=moran_test_raw]{moran_test_raw()}}
}
