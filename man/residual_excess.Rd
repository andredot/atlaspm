% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{residual_excess}
\alias{residual_excess}
\title{Residual excess of avoidable deaths}
\usage{
residual_excess(
  fit,
  geo,
  covariates = NULL,
  exp_col = NULL,
  n_years = 3,
  threshold = 1.1,
  prob_cutoff = 0.8,
  probs = c(0.025, 0.975)
)
}
\arguments{
\item{fit}{A fitted model, normally M5.}

\item{geo}{The \code{sf} it was fitted to.}

\item{covariates}{Covariate columns. Taken from the fit when \code{NULL}.}

\item{exp_col}{Expected-count column. Taken from the fit when \code{NULL}.}

\item{n_years}{Number of years the death counts span, used to annualise.
Default \code{3}.}

\item{threshold}{Residual relative-risk threshold for the exceedance
probability. Default \code{1.10}.}

\item{prob_cutoff}{Probability above which an area is flagged. Default
\code{0.80} (Richardson et al. 2004).}

\item{probs}{Credible-interval bounds. Default \code{c(0.025, 0.975)}.}
}
\value{
A list:
\describe{
\item{\code{total}}{One-row tibble: annualised excess deaths with credible
interval, as a count and as a percentage of all avoidable deaths.}
\item{\code{per_area}}{One row per area: excess, credible interval, residual
RR, exceedance probability and the flag.}
\item{\code{n_flagged}}{Number of areas above \code{prob_cutoff}.}
\item{\code{raw_total}}{The arithmetic version, \code{sum(y) - sum(pred)}, for
comparison.}
}
}
\description{
The primary estimand. For each area, the number of deaths occurring beyond
what its age-sex structure (in the offset) and the model's covariates jointly
predict, with a posterior credible interval; and the total across the study
area, annualised.
}
\details{
Also returns the residual relative risk - the exponentiated area random
effect - and the posterior probability that it exceeds \code{threshold}. This is
the exceedance quantity Results reports at a probability cutoff of 0.80,
and it differs from \code{\link[=augment_bym2]{augment_bym2()}}'s \code{bym2_exceed}: that one is the
probability that \emph{total} relative risk exceeds the threshold, this one is the
probability that risk exceeds the threshold \emph{after} the covariates have had
their say.
}
\examples{
\dontrun{
ex <- residual_excess(fit_M5, smr_geo_full, n_years = 3)
ex$total
subset(ex$per_area, flagged)
}
}
\seealso{
\code{\link[=variance_decomposition]{variance_decomposition()}}, \code{\link[=augment_bym2]{augment_bym2()}}
}
