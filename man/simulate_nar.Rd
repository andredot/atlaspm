% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nar_density.R
\name{simulate_nar}
\alias{simulate_nar}
\title{Simulate a miniature NAR export (development/testing only)}
\usage{
simulate_nar(
  n_residents = 2000,
  n_gps = 25,
  n_areas = 8,
  study_start = as.Date("2022-01-01"),
  study_end = as.Date("2024-12-31"),
  p_never_assigned = 0.04,
  p_gp_retires = 0.15,
  reassignment_gap_days = c(10L, 60L),
  p_dies = 0.06,
  seed = 42L
)
}
\arguments{
\item{n_residents, n_gps, n_areas}{Integers. Population sizes.}

\item{study_start, study_end}{Dates bounding the study window.}

\item{p_never_assigned}{Share of residents never assigned to any GP.}

\item{p_gp_retires}{Share of GPs retiring in-window (orphaning their list).}

\item{reassignment_gap_days}{Length-2 integer: min/max administrative gap
before a retired GP's patients are reassigned.}

\item{p_dies}{Share of residents dying during the window.}

\item{seed}{RNG seed.}
}
\value{
list of tibbles:
\describe{
\item{residents}{\code{resident_id}, \code{area_id}.}
\item{gps}{\code{gp_id}, \code{birth_year}.}
\item{events}{\code{resident_id}, \code{event_date}, \code{event_type}
(\code{"assignment"}, \code{"revocation"}, \code{"death"}), \code{gp_id} (NA except on
assignments).}
}
}
\description{
Mimics the table the SAS extraction produces: for every resident with any
assistance history, one anchor event before \code{study_start} (or a first
assignment during the window), zero or more in-window changes, and — for a
fraction of residents — a \code{death} event that cleanly ends both assistance
and observation. A fraction of residents is never assigned (structural
unassigned); they still appear in \code{residents} and contribute unassigned
person-time while alive.
}
