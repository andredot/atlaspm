% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{build_stroke_area}
\alias{build_stroke_area}
\title{Aggregate section travel times to modelling areas}
\usage{
build_stroke_area(
  stroke_times,
  thresholds = c(mothership = 45, helicopter = 60)
)
}
\arguments{
\item{stroke_times}{Tibble from \code{\link[=build_stroke_times]{build_stroke_times()}}.}

\item{thresholds}{Named numeric vector of minute thresholds. Defaults to the
DGR's own: 45 minutes for mothership centralisation, 60 for helicopter.}
}
\value{
A tibble with one row per \code{area}.
}
\description{
Population weighting is applied to the \emph{times}, not to the geometry: each
section carries its own travel time and its own resident count, and the
weighted statistics are formed at this step.
}
\details{
Section populations need not reconcile with \code{pop_area_table}; they are used
only as relative weights within an area, so a different census vintage
changes nothing material.

\code{pop_share_over_45min_hub} is the covariate worth reaching for first. It is
bounded on \verb{[0, 1]}, which behaves better in a BYM2 linear predictor than a
raw mean in minutes, and it maps directly onto the DGR's own mothership
criterion rather than onto an arbitrary threshold.
}
