% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/describe.R
\name{desc_stats}
\alias{desc_stats}
\title{Every scalar the Results prose interpolates}
\usage{
desc_stats(deaths, arms = attr(deaths, "arms"), n_top = 5, n_years = 3)
}
\arguments{
\item{deaths}{One row per decedent, from \code{\link[=build_deaths]{build_deaths()}}.}

\item{arms}{The long arm table, one row per (death, type, mechanism). Taken
from the \code{"arms"} attribute of \code{deaths} by default. Required, because the
weighted mechanism totals cannot be recovered from the collapsed mechanism
columns: a breast cancer death reads "Screening + Tertiary prevention" at
weight 1 there, when it is half a death in each of two real categories.}

\item{n_top}{Number of leading causes to return. Default \code{5}.}

\item{n_years}{Study duration in years, for annualised figures. Default \code{3}.}
}
\value{
A named list of scalars and small tibbles. See the examples for the
naming convention.
}
\description{
One call, one named list. Adding a number to the thesis text means adding it
here, which keeps the text and the pipeline in lockstep.
}
\details{
Preventable and treatable percentages are deliberately allowed to sum to more
than 100: a death from one of the seven causes split across the two lists is
\emph{at least partially} both, which is how the draft phrases it. The
\code{avoidability} breakdown, which does sum to 100, is returned alongside.
}
\examples{
\dontrun{
d <- desc_stats(deaths_area)
d$n_total; d$median_age; d$top_causes
}
}
