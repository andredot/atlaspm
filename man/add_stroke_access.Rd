% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{add_stroke_access}
\alias{add_stroke_access}
\title{Attach stroke accessibility covariates to the modelling sf}
\usage{
add_stroke_access(
  shp,
  stroke_area,
  vars = c("t_centre_mean", "t_hub_mean", "t_hub_p90", "pop_share_over_45min_hub"),
  scale_vars = c("t_centre_mean", "t_hub_mean")
)
}
\arguments{
\item{shp}{\code{sf} of modelling areas (typically \code{smr_geo}).}

\item{stroke_area}{Tibble from \code{\link[=build_stroke_area]{build_stroke_area()}}.}

\item{vars}{Raw covariate columns to attach.}

\item{scale_vars}{Subset of \code{vars} to additionally attach as \verb{_z} columns,
standardised across the modelled areas only.}
}
\value{
\code{shp} with the requested columns joined on \code{area}.
}
\description{
Standardisation happens here, not in \code{\link[=build_stroke_area]{build_stroke_area()}}, and deliberately
so. \code{stroke_area} covers every Lombardy area the census sections touch,
while the model is fitted on a subset. Z-scoring upstream would centre the
covariate on the regional mean rather than on the modelled one, leaving it
off-centre in the linear predictor and giving the coefficient a unit that no
other regressor in the model shares.
}
