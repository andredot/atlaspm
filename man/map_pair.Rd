% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke_report.R
\name{map_pair}
\alias{map_pair}
\title{Two maps side by side, sharing one legend}
\usage{
map_pair(p1, p2, titles = NULL, position = "bottom")
}
\arguments{
\item{p1, p2}{ggplots, normally both from \code{\link[=plot_smr_map]{plot_smr_map()}}.}

\item{titles}{Optional length-2 character vector of panel titles.}

\item{position}{Where to put the collected legend.}
}
\value{
A patchwork object, or \code{p1} with a message if patchwork is absent.
}
\description{
For raw-versus-smoothed comparisons, where the whole point is that the two
surfaces are on the same scale. Collecting the guide is not cosmetic: two
separate legends invite the reader to compare colours that happen to look
alike but belong to different scales, which is the specific mistake this
pairing exists to prevent.
}
\examples{
\dontrun{
map_pair(plot_smr_map(g, "cvd_smr"), plot_smr_map(aug_cvd, "bym2_rr"),
         titles = c("Raw", "Smoothed"))
}
}
