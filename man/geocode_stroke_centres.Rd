% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{geocode_stroke_centres}
\alias{geocode_stroke_centres}
\title{Geocode stroke centres}
\usage{
geocode_stroke_centres(
  centres,
  registry = NULL,
  overrides = list(),
  sim_min = 0.85
)
}
\arguments{
\item{centres}{Tibble from \code{\link[=import_stroke_centres]{import_stroke_centres()}}.}

\item{registry}{Optional tibble from the regional accredited-facilities
dataset, with columns \code{reg_name}, \code{reg_comune}, \code{reg_lat}, \code{reg_lon}. When
\code{NULL}, every centre goes to Nominatim.}

\item{overrides}{Named list mapping \code{facility} strings to \code{c(lat, lon)}.
Applied last and unconditionally. This is where the handful of cases
automated geocoding cannot resolve are pinned down by hand.}

\item{sim_min}{Minimum Jaro-Winkler similarity to accept a registry match.}
}
\value{
An \code{sf} POINT object in EPSG:4326 with a \code{geocode_source} column.
}
\description{
Resolves each centre to a coordinate pair, preferring the Regione Lombardia
registry of accredited facilities over Nominatim.
}
\details{
Fuzzy name matching against the registry is constrained to candidates in the
comune the DGR assigns to the centre. Name similarity alone produces
confident nonsense here: the region contains several facilities called
"Ospedale Civile", and a 0.9 Jaro-Winkler score between two of them is not
evidence of anything.
}
