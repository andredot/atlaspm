% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{test_conditional_independence}
\alias{test_conditional_independence}
\title{Test a conditional-independence implication on model residuals}
\usage{
test_conditional_independence(fit, geo, var, method = "spearman")
}
\arguments{
\item{fit}{The model omitting \code{var} (normally M4).}

\item{geo}{The \code{sf} it was fitted to.}

\item{var}{Column in \code{geo} to correlate the residuals against.}

\item{method}{Correlation method. Default \code{"spearman"}, which does not
assume the residuals are normal.}
}
\value{
A one-row tibble: \code{variable}, \code{estimate}, \code{p_value}, \code{method}, \code{n}.
}
\description{
The DAG implies that primary care capacity is conditionally independent of
the outcome given deprivation and location. A testable version: after fitting
a model that contains deprivation and the spatial term but \emph{not} primary
care, the residuals should be uncorrelated with primary care capacity.
}
\details{
This is a \emph{testable implication}, not a test of the DAG. Failing it says the
graph is wrong somewhere; passing it says only that this particular
implication survived, which is weaker than it sounds and should be reported
as such.
}
