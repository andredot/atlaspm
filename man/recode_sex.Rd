% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{recode_sex}
\alias{recode_sex}
\title{Map register sex codes onto the ISTAT numeric convention}
\usage{
recode_sex(x, allow_na = FALSE)
}
\arguments{
\item{x}{Character, factor or numeric vector of sex codes. Accepts
\code{"M"}/\code{"F"}, \code{"MASCHIO"}/\code{"FEMMINA"}, \code{"1"}/\code{"2"} and \code{1}/\code{2}, in any case
and with surrounding whitespace.}

\item{allow_na}{Whether missing values are permitted. Default \code{FALSE}.}
}
\value{
Integer vector of \code{1L} (male) and \code{2L} (female).
}
\description{
The ReNCaM extract codes sex as \code{"M"}/\code{"F"}; \code{pop_finale.csv} and
\code{pop_nil.csv} use the ISTAT convention \code{1 = Maschi}, \code{2 = Femmine}. Indirect
standardisation joins deaths to population on this code, so the two sides
must agree. Anything unrecognised is a hard error rather than an \code{NA}: a
silently dropped sex would bias every expected count without warning.
}
\examples{
recode_sex(c("M", "F", "m"))
}
