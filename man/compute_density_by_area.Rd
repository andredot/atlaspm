% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nar_density.R
\name{compute_density_by_area}
\alias{compute_density_by_area}
\title{Person-time GP density by area, computed month by month}
\usage{
compute_density_by_area(
  spells,
  residents,
  study_start,
  study_end,
  l_min = 500,
  l_max = 2000,
  artifact_min_patients = 2L,
  gps = NULL,
  progress = TRUE
)
}
\arguments{
\item{spells}{Tibble/lazy table: \code{resident_id}, \code{gp_id}, \code{spell_start},
\code{spell_end}.}

\item{residents}{Tibble/lazy table: \code{resident_id}, \code{area_id}, \code{obs_end}
(from \code{\link[=build_observation]{build_observation()}}).}

\item{study_start, study_end}{Dates bounding the monthly grid.}

\item{l_min, l_max, artifact_min_patients}{Passed to \code{\link[=clamp_supply]{clamp_supply()}}.}

\item{gps}{Optional tibble \code{gp_id}, \code{birth_year} for the 65+ measure.}

\item{progress}{Logical.}
}
\value{
list(density, by_month, clamp_report, assumptions):
\describe{
\item{density}{Per \code{area_id}: \code{person_months} (alive person-time),
\code{gp_density}, \code{gp_density_per_100k}, \code{mean_list_size_assigned}
(observed, unclamped), \code{prop_unassigned} (share of ALIVE person-time
unassigned; artifact months included), \code{prop_artifact}, \code{gps_fte},
optional \code{prop_gp_65plus}.}
\item{by_month}{Same per \code{area_id} x \code{month}, incl. \code{n_alive} — use
it to inspect retirement-churn windows and mortality-driven
denominator decline.}
\item{clamp_report}{Per month x \code{clamp_status}: GPs and person-months
affected — the audit trail of the assumptions.}
\item{assumptions}{One-row echo of all thresholds and the implied
supply bounds, plus the boundary-closure flag.}
}
}
\description{
For each month m (attributed by its first day):
\enumerate{
\item denominator: residents under observation at m (\code{obs_end >= m}) —
the dead have exited cleanly;
\item numerator inputs: spells active at m (<= 1 row per living resident);
\item observed monthly list sizes -> \code{\link[=clamp_supply]{clamp_supply()}} (visible assumptions);
\item per-area aggregates: assigned/artifact person-months, summed supply,
list-size components; unassigned = alive - assigned.
}
}
\details{
\code{spells} and \code{residents} may be tibbles or dbplyr lazy tables (DuckDB,
Postgres, SQLite): every verb inside the loop translates to SQL, so only
small aggregates cross into R each month. NOTE on database date columns:
store dates as native DATE (DuckDB, Postgres) or ISO-8601 text (SQLite,
which has no DATE type); integer epoch-days will compare wrongly against
the Date literals used here. Verified to give identical results on
tibbles and on a SQL backend.

Progress: a cli progress bar (message() fallback) advances per month; a
closing summary reports the person-time affected by each assumption.
}
