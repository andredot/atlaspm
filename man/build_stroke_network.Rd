% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{build_stroke_network}
\alias{build_stroke_network}
\title{Build a routable street network with the AREU speed model}
\usage{
build_stroke_network(
  aoi,
  urban_mask = NULL,
  speed_model = c("areu", "osm"),
  osm_dir = "data/osm",
  place = "Lombardia",
  speeds = c(urban = 33, extra_urban = 60, motorway = 90)
)
}
\arguments{
\item{aoi}{\code{sf} or \code{sfc} polygon bounding the area to route within. Must
extend well beyond the study area or routes that legitimately leave it
will be truncated.}

\item{urban_mask}{\code{sfc} from \code{\link[=build_urban_mask]{build_urban_mask()}}. Required when
\code{speed_model = "areu"}.}

\item{speed_model}{One of \code{"areu"} or \code{"osm"}.}

\item{osm_dir}{Directory for the cached \code{.pbf}.}

\item{place}{Geofabrik place name passed to \code{\link[osmextract:oe_get]{osmextract::oe_get()}}.}

\item{speeds}{Named numeric vector of km/h for the AREU model.}
}
\value{
A contracted \code{dodgr} graph (a data frame).
}
\description{
Downloads a Geofabrik OSM extract and builds a contracted \code{dodgr} graph. No
routing server and no Docker: everything runs in-process.
}
\details{
\code{speed_model = "areu"} imposes the assumptions Regione Lombardia used to
draw the DGR's own centralisation maps (Figs. 4-6 of the Allegato): 33 km/h
urban, 60 km/h extra-urban, 90 km/h motorway. Use it if the resulting
covariate is to be commensurable with the regional 45-minute mothership
threshold. \code{"osm"} keeps dodgr's stock motorcar profile, which models a
private car in free flow and is \emph{not} comparable to the DGR figures.

Neither model includes congestion, dispatch delay, or on-scene time. What
comes out is network accessibility, not a realised prehospital interval, and
should be described that way.
}
