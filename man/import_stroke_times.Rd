% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{import_stroke_times}
\alias{import_stroke_times}
\title{Read a previously computed section-level travel-time table}
\usage{
import_stroke_times(file_path, areas = NULL, max_minutes = 600)
}
\arguments{
\item{file_path}{Path to the saved table. \code{.rds}, \code{.csv} and \code{.parquet} are
recognised by extension.}

\item{areas}{Optional character vector of valid modelling-area keys, for
checking. Normally \code{area_shp$area}.}

\item{max_minutes}{Upper bound for a plausible travel time, used only to
catch a units mismatch. Default \code{600}.}
}
\value{
A tibble with the columns \code{\link[=build_stroke_area]{build_stroke_area()}} expects: \code{area},
\code{pop}, \code{t_centre_min}, \code{t_hub_min}, \code{nearest_centre}, \code{nearest_hub}, plus
whatever else the file carries.
}
\description{
Restores the output of \code{\link[=build_stroke_times]{build_stroke_times()}} from a file, so the pipeline
can resume from the routing results without rebuilding the road network.
}
\details{
\strong{Why this exists.} \code{\link[=build_stroke_network]{build_stroke_network()}} takes 10-40 minutes and
8-16 GB, and needs an OSM extract on disk. Nothing downstream of
\code{stroke_times} depends on the network object itself, so once the routing has
been run and its output saved, every subsequent analysis can start from the
table. The routing functions stay in the package and stay tested; only the
pipeline's entry point moves.

\strong{What it validates.} The columns \code{\link[=build_stroke_area]{build_stroke_area()}} consumes, and
the two failure modes that would otherwise pass silently: an \code{area} key that
does not match the modelling geography (aggregation would drop those
sections), and travel times that are not plausibly minutes. A file saved
from an older run with seconds, or with kilometres, would aggregate happily
and produce an accessibility surface wrong by a constant factor.
}
\examples{
\dontrun{
stroke_times <- import_stroke_times(
  get_input_data_path("stroke_times.rds"), areas = area_shp$area
)
}
}
\seealso{
\code{\link[=build_stroke_times]{build_stroke_times()}}, \code{\link[=build_stroke_area]{build_stroke_area()}}
}
