% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke_report.R
\name{smoothing_summary}
\alias{smoothing_summary}
\title{What the BYM2 smoothing did to an outcome surface}
\usage{
smoothing_summary(
  geo,
  aug,
  diag,
  obs_col = "cvd_obs",
  exp_col = "cvd_exp",
  rr_col = "bym2_rr"
)
}
\arguments{
\item{geo}{The \code{sf} carrying the observed and expected counts.}

\item{aug}{Output of \code{\link[=augment_bym2]{augment_bym2()}} for the same outcome.}

\item{diag}{Output of \code{\link[=check_bym2_fit]{check_bym2_fit()}} for the same fit.}

\item{obs_col, exp_col}{Observed and expected count columns in \code{geo}.}

\item{rr_col}{Smoothed relative-risk column in \code{aug}.}
}
\value{
A two-column tibble, \code{Quantity} and \code{Value}, ready for \code{kable()}.
}
\description{
The raw ratio and the smoothed relative risk are the same quantity seen
before and after shrinkage, and the gap between their ranges is the honest
statement of how much of the raw map was noise. Reporting the two ranges
side by side with \code{rho} stops a reader taking a dramatic raw map at face
value: with a median of two or three expected deaths per area, one extra
death moves a small unit's ratio by a large fraction.
}
\examples{
\dontrun{
smoothing_summary(smr_geo_allage, aug_i63, diag_i63_bym2,
                  obs_col = "i63_obs", exp_col = "i63_exp")
}
}
\seealso{
\code{\link[=exceedance_count]{exceedance_count()}}
}
