% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke_report.R
\name{build_icd_outcome}
\alias{build_icd_outcome}
\title{Build an indirectly standardised outcome for an arbitrary ICD set}
\usage{
build_icd_outcome(
  mort_raw,
  pop_area_table,
  areas,
  prefixes,
  age_min = 0,
  age_max = Inf,
  pop_year = 2022:2024,
  label = "outcome"
)
}
\arguments{
\item{mort_raw}{Output of \code{\link[=import_mortality]{import_mortality()}} - the unfiltered register,
before \code{\link[=preprocess_mortality]{preprocess_mortality()}} applies the age and cause restrictions.}

\item{pop_area_table}{Population by area, age, sex and year.}

\item{areas}{Character vector of study areas to retain.}

\item{prefixes}{ICD-10 prefixes to match, e.g. \code{"I63"} or \code{c("I60","I61")}.
Matched against the leading characters of the normalised cause code, so
\code{"I63"} captures I63.0 through I63.9.}

\item{age_min, age_max}{Age range, inclusive. Defaults to all ages.}

\item{pop_year}{Years to sum for person-years.}

\item{label}{Stem for the output columns.}
}
\value{
A tibble: \code{area}, \verb{<label>_obs}, \verb{<label>_exp}, \verb{<label>_smr}.
Attributes record the definition used.
}
\description{
Bypasses the avoidable-cause lookup entirely and standardises a chosen set of
ICD-10 prefixes over any age range, against the same population denominator
the rest of the study uses.
}
\details{
The standard is internal, as elsewhere: age-sex specific rates are computed
across the whole study territory and applied to each area's own age-sex
structure. The expected counts are therefore on the same footing as
\code{total_exp}, and the resulting SMRs are comparable with the main analysis
even though the age range differs.
}
\examples{
\dontrun{
i63 <- build_icd_outcome(mort_raw, pop_area_table, area_shp$area,
                         prefixes = "I63", label = "i63")
}
}
\seealso{
\code{\link[=outcome_feasibility]{outcome_feasibility()}}, \code{\link[=preprocess_smr]{preprocess_smr()}}
}
