% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{stack_by_model}
\alias{stack_by_model}
\title{Stack per-model results, failing loudly on missing ones}
\usage{
stack_by_model(x, id = "model", require_all = FALSE)
}
\arguments{
\item{x}{Named list of one-row data frames, one per model.}

\item{id}{Name of the identifier column. Default \code{"model"}.}

\item{require_all}{Error if any element is missing, rather than warning and
stacking the rest. Default \code{FALSE}.}
}
\value{
A tibble with an \code{id} column, one row per non-empty element.
}
\description{
\code{dplyr::bind_rows()} on a list of \code{NULL}s returns a tibble with zero rows
\strong{and zero columns}, which is the worst possible failure mode here: the
pipeline reports success, the target is stored, and the error only surfaces
much later inside the report as "Column \code{statistic} doesn't exist", pointing
at the report rather than at the model that actually failed.
}
\details{
This makes the absence explicit and names the models responsible.
}
\examples{
stack_by_model(list(M1 = data.frame(x = 1), M2 = data.frame(x = 2)))
}
