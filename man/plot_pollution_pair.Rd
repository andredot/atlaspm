% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_report.R
\name{plot_pollution_pair}
\alias{plot_pollution_pair}
\title{NO2 and PM2.5 side by side}
\usage{
plot_pollution_pair(geo, shared_scale = FALSE, caption = NULL, cor_note = TRUE)
}
\arguments{
\item{geo}{Modelling \code{sf} carrying the pollutant columns. Column names are
detected with \code{\link[=exposure_columns]{exposure_columns()}}, so the exposure year does not need to
be hardcoded.}

\item{shared_scale}{Put both panels on one legend. Default \code{FALSE}.}

\item{caption}{Passed to \code{labs()}; a default noting the source is used when
\code{NULL}.}

\item{cor_note}{Append the rank correlation between the two surfaces to the
caption. Default \code{TRUE}.}
}
\value{
A patchwork object when \code{shared_scale = FALSE} and \pkg{patchwork} is
available, otherwise a single faceted ggplot.
}
\description{
The figure behind the pollutant selection argument. Both surfaces are in the
same units, so two presentations are possible and they answer different
questions.
}
\details{
\strong{Free scales} (the default) show each pollutant's own spatial pattern:
where in the territory each is high. Use this to see whether the two
pollutants pick out the same places.

\strong{Shared scale} puts both on one common ug/m3 legend. This is the
honest version of the selection argument, because it shows directly what the
methods claim - that PM2.5 varies far less across the study area than NO2
does, and therefore carries less contrast for a model to use. On free scales
that difference is invisible, since each panel is stretched to fill its own
range.
}
\examples{
\dontrun{
plot_pollution_pair(smr_geo_full)
plot_pollution_pair(smr_geo_full, shared_scale = TRUE)
}
}
\seealso{
\code{\link[=exposure_columns]{exposure_columns()}}, \code{\link[=plot_travel_time]{plot_travel_time()}}
}
