% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/import_.R
\name{check_gp_density}
\alias{check_gp_density}
\title{Audit the primary-care indicator against the modelling geography}
\usage{
check_gp_density(gp_density_area, area_shp, max_unmatched_pct = 1)
}
\arguments{
\item{gp_density_area}{Output of \code{\link[=import_gp_density]{import_gp_density()}}.}

\item{area_shp}{The modelling geography.}

\item{max_unmatched_pct}{Warn when the share of modelled areas with no
indicator exceeds this. Default \code{1}.}
}
\value{
A one-row tibble: \code{n_areas}, \code{n_indicator}, \code{n_matched},
\code{n_area_no_indicator}, \code{n_indicator_no_area}, \code{pop_unmatched},
\code{pct_pop_unmatched}. The unmatched keys are attached as the
\code{"area_no_indicator"} and \code{"indicator_no_area"} attributes.
}
\description{
The indicator and \code{area_shp} are built from different registers, so they
need not cover the same areas, and a mismatch is invisible downstream:
\code{\link[=add_covariate]{add_covariate()}} imputes an unmatched area from its neighbours' mean and
only warns, so a systematic gap would enter the model as smoothed
neighbouring values rather than as an error. This reports the overlap in
both directions and quantifies how much population sits outside it.
}
\details{
Two mismatches are expected against the current extract and are not faults:
the indicator carries a bare \code{015146} row for Milan residents whose NIL did
not resolve, and a row for NIL 8 (Parco Sempione), which the pipeline drops
for having no resident population. Both are tiny; the point of the audit is
that their size is reported rather than assumed.
}
\examples{
\dontrun{
check_gp_density(gp_density_area, area_shp)
}

}
\seealso{
\code{\link[=import_gp_density]{import_gp_density()}}
}
