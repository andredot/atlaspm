% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{build_section_points}
\alias{build_section_points}
\title{Build population-weighted origin points from census sections}
\usage{
build_section_points(
  sez_shp,
  area_shp,
  pop_col = "P1",
  drop_unpopulated = TRUE
)
}
\arguments{
\item{sez_shp}{\code{sf} polygons of ISTAT census sections.}

\item{area_shp}{\code{sf} polygons of modelling areas, carrying an \code{area} column.}

\item{pop_col}{Name of the resident population column in \code{sez_shp}.}

\item{drop_unpopulated}{Drop sections with zero residents. They contribute
nothing to a population-weighted statistic and are typically a third of
all sections, so this is the cheapest speed-up in the pipeline.}
}
\value{
A tibble with one row per populated section: \code{sez_id}, \code{area},
\code{pop}, \code{lon}, \code{lat}.
}
\description{
Census sections are the finest geography ISTAT publishes population for.
Routing from them, rather than from an area centroid, is what makes the
population weighting meaningful: travel time is convex over a road network,
so the time \emph{from} a weighted centroid systematically understates the
population-weighted \emph{mean} of times. The gap is negligible in compact
comuni and material wherever settlement is strung out, which is precisely
where an accessibility covariate is supposed to carry signal.
}
\details{
Sections are assigned to modelling areas by spatial join against \code{area_shp}
rather than by reconstructing the composite \code{area} key. This guarantees the
origin layer partitions exactly the same areas the model is fitted on, and
survives any future change to how that key is built.
}
