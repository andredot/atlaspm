% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{augment_bym2_mechanisms}
\alias{augment_bym2_mechanisms}
\title{Collect per-mechanism smoothed RR (and exceedance) into one wide sf}
\usage{
augment_bym2_mechanisms(geo, fits, threshold = 1.1, out_suffix = "_bym2")
}
\arguments{
\item{geo}{The same \code{sf} passed to \code{\link{fit_bym2_mechanisms}}.}

\item{fits}{The named list returned by \code{\link{fit_bym2_mechanisms}}.}

\item{threshold}{Relative-risk threshold for the exceedance columns. Default
\code{1.10}.}

\item{out_suffix}{Suffix for the smoothed-RR columns. Default \code{"_bym2"}.}
}
\value{
\code{geo} with \code{<stem>_bym2} and \code{<stem>_bym2_exc} columns
added, and the threshold recorded as an attribute.
}
\description{
Augments \code{geo} with one smoothed-relative-risk column per mechanism,
named \code{<stem><out_suffix>} (default \code{<stem>_bym2}), plus a matching
exceedance column \code{<stem><out_suffix>_exc} = P(RR > \code{threshold}).
The result is shaped exactly like the input to \code{\link{plot_smr_facets}},
so the faceted small-multiples map needs no new plotting code - just point its
\code{cols} at the \code{_bym2} columns.
}
\examples{
\dontrun{
smr_geo_mech_bym2 <- augment_bym2_mechanisms(smr_geo, fits)
plot_smr_facets(smr_geo_mech_bym2,
                cols   = dplyr::matches("^M_.*_bym2$"),
                breaks = c(-Inf, 0.90, 0.95, 1.05, 1.10, Inf),
                strip_suffix = "_bym2$")
}
}
