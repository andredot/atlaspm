% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{collect_diagnostics}
\alias{collect_diagnostics}
\title{Convergence and sampler diagnostics for a set of fits}
\usage{
collect_diagnostics(fits, labels = names(fits))
}
\arguments{
\item{fits}{Named list of fitted models.}

\item{labels}{Optional display labels, defaulting to \code{names(fits)}.}
}
\value{
A tibble, one row per model: \code{model}, \code{n_draws}, \code{rhat_max},
\code{ess_min}, \code{n_divergent}, \code{pct_divergent}, \code{treedepth_hits}, \code{ebfmi_min},
\code{rho}, \code{waic}.
}
\description{
The table behind Results "Model comparison": \emph{"All models converged:
rank-normalised R-hat below 1.01, bulk and tail effective sample sizes above
[N] for every parameter, [N] divergent transitions."}
}
\details{
Runs \code{\link[=check_bym2_fit]{check_bym2_fit()}} over a named list and stacks the results, so the
claim can be checked against every model at once rather than eyeballed one
at a time.
}
\examples{
\dontrun{
collect_diagnostics(list(M3 = fit_M3, M5 = fit_M5))
}
}
