% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarise.R
\name{collect_coefficients}
\alias{collect_coefficients}
\title{Coefficient table across models, on the relative-risk scale}
\usage{
collect_coefficients(
  fits,
  term_labels = NULL,
  exponentiate = TRUE,
  probs = c(0.025, 0.975)
)
}
\arguments{
\item{fits}{Named list of fitted models.}

\item{term_labels}{Optional named character vector mapping variable names to
display labels, e.g. \code{c(di_score_z = "Deprivation (per SD)")}.}

\item{exponentiate}{Return relative risks rather than log-scale
coefficients. Default \code{TRUE}.}

\item{probs}{Credible-interval bounds.}
}
\value{
A tibble: \code{model}, \code{term}, \code{label}, \code{estimate}, \code{ci_low}, \code{ci_high},
\code{rhat}, \code{ess}, and \code{crosses_null} (whether the interval contains 1, or 0 on
the log scale).
}
\description{
Covariates are standardised before fitting, so each coefficient is the
multiplicative change in relative risk per one standard deviation of the
covariate. Exponentiating is therefore the only presentation that means
anything to a reader, and it is the default.
}
\details{
Term names are recovered from each fit's specification (see \code{\link[=fit_model]{fit_model()}}),
so \code{beta[1]} becomes \code{di_score_z} automatically and the table cannot silently
mislabel a coefficient when a formula changes.
}
\examples{
\dontrun{
collect_coefficients(list(M4 = fit_M4, M5 = fit_M5),
                     term_labels = c(no2_z = "NO2 (per SD)"))
}
}
