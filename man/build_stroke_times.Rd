% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stroke.R
\name{build_stroke_times}
\alias{build_stroke_times}
\title{Compute travel time from every census section to the nearest stroke centre}
\usage{
build_stroke_times(network, centres, sections, max_snap_m = 500)
}
\arguments{
\item{network}{Contracted \code{dodgr} graph from \code{\link[=build_stroke_network]{build_stroke_network()}}.}

\item{centres}{\code{sf} from \code{\link[=geocode_stroke_centres]{geocode_stroke_centres()}}, carrying \code{is_hub},
\code{lon} and \code{lat}.}

\item{sections}{Tibble from \code{\link[=build_section_points]{build_section_points()}}, carrying \code{lon}, \code{lat},
\code{pop} and \code{area}.}

\item{max_snap_m}{Warn when a centre snaps further than this to the network.}
}
\value{
A tibble: one row per section, with \code{t_centre_min}, \code{t_hub_min}, the
identity of the nearest centre and hub, and the \code{area} key.
}
\description{
Routes one-to-many from each of the 41 stroke centres across the network,
which is 41 Dijkstra runs rather than one per census section.
}
\details{
Because the graph is not reversed, what is computed is the time \emph{from} each
centre \emph{to} each section, and it is used as an estimate of the journey in
the opposite direction. The two differ only where one-way restrictions make
the return leg longer. Against a speed model with no congestion term and
33/60/90 km/h class speeds, that discrepancy sits well below the resolution
of the measure, but it is an approximation and should be described as one.
}
