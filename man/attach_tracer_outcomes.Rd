% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{attach_tracer_outcomes}
\alias{attach_tracer_outcomes}
\title{Attach the tracer and control outcome columns}
\usage{
attach_tracer_outcomes(
  geo,
  tracer_cause = "Cerebrovascular diseases",
  nco_group = "Cancer",
  positive_mechanism = "Lifestyle and NCDs"
)
}
\arguments{
\item{geo}{The modelling \code{sf} carrying the wide SMR columns.}

\item{tracer_cause}{Cause label for the tracer. Default
\code{"Cerebrovascular diseases"}.}

\item{nco_group}{Group label for the negative control outcome. Default
\code{"Cancer"} (all cancer, not lung cancer alone).}

\item{positive_mechanism}{Mechanism label for the positive control. Default
\code{"Lifestyle and NCDs"}.}
}
\value{
\code{geo} with \code{cvd_obs}/\code{cvd_exp}, \code{cancer_obs}/\code{cancer_exp} and
\code{poscontrol_obs}/\code{poscontrol_exp} added.
}
\description{
\code{preprocess_smr()} already emits an observed/expected pair for every cause,
group and mechanism; this selects the four pairs the control analysis needs
and gives them short, stable names.
}
\details{
Column names are \emph{derived} through \code{\link[=smr_col]{smr_col()}} rather than hardcoded,
because \code{janitor::make_clean_names()} transliterates in ways that are not
guessable - "Lifestyle and NCDs" becomes \code{lifestyle_and_nc_ds}, not
\code{lifestyle_and_ncds}. Hardcoding would work until the day someone edits a
label in the lookup.

A note on the counts. Cerebrovascular disease is one of the seven causes
split across the preventable and treatable lists, so each death contributes
two rows of \code{weight = 0.5}. Both rows share the same \code{cause} label, so
\code{preprocess_smr()}'s cause-level aggregation sums them back to 1.0 and
\code{cvd_obs} is a whole death count, not a half one. The same holds for
\code{cancer_obs}, where colorectal and breast cancer are split across mechanisms
but share a group.
}
\seealso{
\code{\link[=fit_controls]{fit_controls()}}
}
