% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/controls.R
\name{fit_controls}
\alias{fit_controls}
\title{Fit the tracer and control models}
\usage{
fit_controls(
  geo,
  C,
  scale_factor,
  exposure = "t_hub_mean_z",
  deprivation = "di_score_z",
  adjust = character(0),
  engine = "glm",
  nce_exposure = NULL,
  tracer_obs = "cvd_obs",
  tracer_exp = "cvd_exp",
  ...
)
}
\arguments{
\item{geo}{Output of \code{\link[=attach_tracer_outcomes]{attach_tracer_outcomes()}}, with the exposure columns
attached by \code{\link[=add_stroke_access]{add_stroke_access()}} and \code{\link[=add_covariate]{add_covariate()}}.}

\item{C}{Adjacency matrix.}

\item{scale_factor}{Shared BYM2 scale factor.}

\item{exposure}{Travel-time exposure column. Default \code{"t_hub_mean_z"}.}

\item{deprivation}{Deprivation column for the positive control. Default
\code{"di_score_z"}.}

\item{adjust}{Covariates to adjust for, applied identically to the tracer and
the negative control outcome. The DAG for Objective 3 has one backdoor
path, \verb{A <- hub siting <- U -> Y}, where \code{U} is the latent urban-rural
position; these are its measured children on the outcome side, so
adjusting for them is proxy control for \code{U}.}

\item{engine}{Estimator, passed to \code{\link[=fit_model]{fit_model()}}. Defaults to \code{"glm"}. A
spatial random effect is a nonparametric estimate of that same \code{U}, and
because travel time is very nearly a deterministic function of position,
conditioning on a smooth spatial surface conditions on the exposure. The
BYM2 coefficient is then the effect of the exposure's non-spatial residual,
which is a different estimand - fit it as a sensitivity bound, not here.}

\item{nce_exposure}{Negative control exposure column. \code{NULL} skips T3; see}

\item{tracer_obs, tracer_exp}{Observed and expected counts for the tracer
and, when fitted, the negative control exposure. Default to the
cerebrovascular 0-74 outcome; the pipeline points them at the all-age
cerebral-infarction outcome (\code{i63_obs} / \code{i63_exp}), which is the
reference stroke model reported in the Results.
the note below.}

\item{...}{Passed to \code{\link[=fit_model]{fit_model()}}.}
}
\value{
A named list of fits: \code{tracer}, \code{nc_outcome}, \code{positive}, and
\code{nc_exposure} when fitted.
}
\description{
Fits T1, T2 and T4 (and T3 when \code{nce_exposure} is supplied) on a shared
adjacency graph and scale factor, so that differences between the
coefficients cannot be attributed to differences in the smoothing.
}
\details{
Every model uses the same BYM2 engine and the same exposure column, so the
comparison across them is a comparison of outcomes, which is the only thing
the negative-control argument licenses.
}
\section{The negative control exposure}{

T3 is deliberately not given a default. A negative control exposure has to be
chosen on substantive grounds - it must share the confounding structure of
travel-time-to-hub while being incapable of affecting stroke case fatality -
and picking one silently would give the analysis an unearned appearance of
completeness. Until it is chosen, \code{nce_exposure} stays \code{NULL} and the
control panel reports the slot as not yet filled.
}

