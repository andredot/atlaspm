% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_.R
\name{plot_scatter_smr_ivsm}
\alias{plot_scatter_smr_ivsm}
\title{Standardised mortality rate vs social/material vulnerability scatter}
\usage{
plot_scatter_smr_ivsm(
  smr,
  ivsm,
  group_var = "comune",
  isr = "total_isr",
  ivsm_col = "ivsm",
  smooth = TRUE,
  title = "Standardised mortality vs social/material vulnerability, by comune",
  subtitle =
    "Each point a comune; x = indirectly standardised rate, y = IVSM (national average = 100)"
)
}
\arguments{
\item{smr}{Standardised table from \code{preprocess_smr()} (one row per
comune), supplying the standardised rate.}

\item{ivsm}{IVSM table from \code{import_ivsm()} (one row per comune).}

\item{group_var}{Comune key present in both. Default \code{"comune"}.}

\item{isr}{Column in \code{smr} holding the overall standardised rate.
Default \code{"total_isr"}.}

\item{ivsm_col}{Column in \code{ivsm} holding the index. Default \code{"ivsm"}.}

\item{smooth}{Logical; add a linear trend line. Default \code{TRUE}.}

\item{title, subtitle}{Plot annotations.}
}
\value{
A \code{ggplot} object.
}
\description{
One point per comune: the overall indirectly standardised mortality rate
(ISR, \emph{not} the SMR ratio) on the x-axis and the ISTAT vulnerability
index (IVSM) on the y-axis. A positive slope indicates that more
vulnerable comuni also carry higher standardised mortality. The ISR comes
from \code{\link{preprocess_smr}} (the \code{total_isr} column by default)
and the IVSM from \code{\link{import_ivsm}}; the two are joined on the shared
\code{comune} key.
}
