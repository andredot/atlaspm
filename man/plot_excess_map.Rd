% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_report.R
\name{plot_excess_map}
\alias{plot_excess_map}
\title{Map of residual excess deaths}
\usage{
plot_excess_map(
  excess,
  geo,
  flagged_only = FALSE,
  subtitle = NULL,
  caption = NULL
)
}
\arguments{
\item{excess}{Output of \code{\link[=residual_excess]{residual_excess()}}.}

\item{geo}{The \code{sf} the model was fitted to.}

\item{flagged_only}{Shade only the areas above the probability cutoff,
leaving the rest grey. Default \code{FALSE}.}

\item{subtitle, caption}{Passed to \code{labs()}.}
}
\value{
A ggplot.
}
\description{
The primary output for practice: not the relative risk, but the count of
deaths beyond what the covariates predict. Counts rather than ratios because
a doubled risk in an area of 4,000 people and a 10\% excess in an area of
90,000 mean very different things to a service planner, and the ratio map
hides that.
}
\details{
A diverging scale centred on zero, because the sign matters here in a way it
does not on a relative-risk map.
}
